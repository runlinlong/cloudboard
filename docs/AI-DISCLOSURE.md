# AI assistance and provenance

This repository is an AI-generated review draft, not independently student-authored work. OpenAI Codex generated the Python services, HTML/CSS/JavaScript, deployment files, tests, scripts and document drafts in response to the student's assignment brief and request for an implementation. The student supplied the assignment, course policy and environment constraints. Do not claim otherwise.

The supplied course policy explicitly says: “You MUST NOT use GenAI tools to write any source code or code documentation.” It also advises against AI-generated reports for the microservices project. It separately permits AI-assisted installation, explanation, planning and configuration examples that the student understands and re-types. Reviewing, lightly editing or merely re-typing AI-generated application code does not by itself resolve the source-code prohibition.

Before any assessed submission, resolve permitted use with the instructor and comply with the course rules. This document discloses provenance; it does not establish permission or guarantee acceptance. The student must independently understand, verify and take responsibility for all submitted material. Replace draft prose with the student's own reasoning wherever required, without erasing truthful attribution.

Prompt themes: implement a small Python microservices assignment; deploy on Kubernetes; expose REST APIs; preserve a separate concurrent Docker coursework environment; use an isolated Python environment; provide deployment, tests, architecture discussion and a video plan. The conversation is the fuller prompt record.

Official sources used for technical checks are listed in ARCHITECTURE.md. The UI uses system fonts and CSS, with no downloaded artwork or icon packages. Python and database dependencies retain their own licenses. Test outcomes must be described only when actually observed; see VERIFICATION.md.
